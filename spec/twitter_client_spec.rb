require 'rails_helper'
require 'spec_helper'

RSpec.describe TwitterClient do
  describe "#user_search" do
    it "returns users matching the search term" do
    end

    it "renders the search page if field is left blank" do
    end

    it "returns results page with message if no results found" do
    end
  end

end
