<% if params[:feed_id] %>
  <%= link_to "Subscribe", subscribe_path(params[:feed_id]), method: "post", class: "btn btn-info btn-md" %>
<% end %>

<% if current_user %>
  <%= render partial: "shared/feed", locals: { posts: @posts, display_author: true } %>
<% end %>
