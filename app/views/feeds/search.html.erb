<!-- Make this a form_for. WTF form_tag? -->
<%= form_tag(search_instagram_path, :method => "get", class: "navbar-form", id: "search-form") do %>
    <div class="input-append">
      <%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search Users" %>
      <button class="btn" type="submit"><i class="icon-search">Search</i></button>
    </div>
<% end %>

<!-- same for Twitter; make it a partial. -->

<!-- Results below. Define @response in feeds controller -->
<h2>Search Results</h2>
<% @response.each do |result| %>
  <p><%= result.screen_name %></p>
<% end %>
