<nav class="header">
  <div class="navbar navbar-default">
    <div class="navbar-header">
      <%= link_to "FeedMe", root_path, class: "navbar-brand news-text" %>
      <p class="news-text"> The news feed you actually care about</p>
    </div>
    <div class="nav navbar-nav navbar-right button">
      <% if session[:user_id] %>
      <p class="news-text">Logged in as: <%= @current_user.name %></p>
        <%= link_to "Logout", signout_path, method: :delete, class: "btn btn-default btn-sm" %>
      <% end %>
    </div>
  </div>
</nav>
