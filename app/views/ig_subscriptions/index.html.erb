<table class="table table-hover">
  <th>Username</th>
  <th>Name</th>
  <th>Pic</th>
  <% @response.each do |person| %>
  <tr>
    <td><%= person["username"] %></td>
    <td><%= person["full_name"] %></td>
    <td><%= image_tag(person["profile_picture"]) %></td>
    </tr>
  <% end %>
</table>
